# Run this command in repository https://gitweb.gentoo.org/repo/gentoo.git/

grep -r 'HOMEPAGE' ./* | awk -F '=' '{print $2}' | sed -e "s/[[:space:]]\+/ /g" | cut -d ' ' -f 2 | sed 's/\"//g'| cut -d '/' -f 1-3 | sort -u > homepages


Smazání ftp://, # a pod.

Sloučení http a https:


Sloučení www a bez www:



reverse < input | sort | reverse | less

Manuálně promazané domény (-> značí webovou stránku, která byla ze skupiny ponechána):

